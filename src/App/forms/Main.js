import React, { Component } from 'react';

export default class MainForm extends Component {
    render() {
        return (
            <form>
                <button>Hello</button>
            </form>
        );
    }
}